# Recipe-Diagram-Builder
A hybrid recipe-to-flowchart web app that uses a custom non-AI parser as the primary engine, with optional AI fallback for complex recipes. Built with Next.js, React and TypeScript, and deployed through Vercel.
